# Server-side only — never exposed to the client.
# Get a key at https://platform.openai.com/ and copy this file to .env.local
OPENAI_API_KEY=sk-...

# Optional: override the generation model (default: gpt-5.1)
# OPENAI_MODEL=gpt-5.1
